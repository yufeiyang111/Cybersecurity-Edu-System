# CyberGuard 网络安全智能问答教学系统 - 技术实现文档

---

## 1. 文档上传功能实现

### 1.1 技术方案

**支持格式**：`.pdf`、`.docx`、`.html`、`.md`、`.txt`

**核心依赖库**：

| 格式 | 依赖库 |
|------|--------|
| PDF | `pdfplumber` (首选), `PyPDF2` (备用) |
| Word | `python-docx` |
| HTML | `beautifulsoup4` |
| Markdown | `markdown` |
| 通用 | `re` (正则), `yaml` (front matter解析) |

**关键代码位置**：`backend/app/services/document_parser.py`

---

### 1.2 核心代码片段

#### 工厂模式解析器选择

```python
class DocumentParserFactory:
    PARSERS = {
        ".pdf": PDFParser,
        ".docx": DocxParser,
        ".doc": DocxParser,
        ".html": HTMLParser,
        ".htm": HTMLParser,
        ".md": MarkdownParser,
        ".txt": TextParser,
    }

    @classmethod
    def get_parser(cls, file_path: str) -> DocumentParser:
        ext = Path(file_path).suffix.lower()
        parser_class = cls.PARSERS.get(ext)
        if parser_class is None:
            raise ValueError(f"不支持的文件格式: {ext}")
        return parser_class()
```

#### Markdown解析器 - 保留原始格式

```python
class MarkdownParser(DocumentParser):
    def parse(self, file_path: str) -> Dict[str, Any]:
        with open(file_path, "r", encoding="utf-8") as f:
            md_content = f.read()

        # 提取YAML front matter
        front_matter = {}
        if md_content.startswith("---"):
            parts = md_content.split("---", 2)
            if len(parts) >= 3:
                import yaml
                front_matter = yaml.safe_load(parts[1]) or {}
                md_content = parts[2]

        # 提取标题（保留原始内容，不移除格式）
        title = ""
        lines = md_content.split("\n")
        for line in lines:
            if line.startswith("# "):
                title = line[2:].strip()
                break

        # 清理多余空行，但保留所有Markdown格式
        text_content = re.sub(r'\n{3,}', '\n\n', md_content)

        return {
            "content": text_content.strip(),
            "metadata": {
                "title": front_matter.get("title", title),
                "tags": front_matter.get("tags", []),
                "author": front_matter.get("author", "")
            },
            "source": os.path.basename(file_path),
            "format": "markdown"
        }
```

#### Word文档解析器 - 输出Markdown格式

```python
class DocxParser(DocumentParser):
    def _paragraph_to_markdown(self, para) -> str:
        """将段落转换为Markdown格式"""
        style_name = para.style.name.lower() if para.style else ""

        # 处理标题样式
        if style_name.startswith("heading"):
            level = int(style_name.replace("heading", "").strip())
            level = min(max(level, 1), 6)  # 限制在1-6级
            text = para.text.strip()
            return f"{'#' * level} {text}\n" if text else ""

        # 处理列表项
        if style_name in ["list bullet", "list bullet 2", "list bullet 3"]:
            text = para.text.strip()
            return f"- {text}\n" if text else ""

        # 普通段落 - 保留加粗、斜体等行内格式
        text = self._get_paragraph_text_with_format(para)
        return f"{text}\n\n" if text.strip() else ""

    def _table_to_markdown(self, table) -> str:
        """将表格转换为Markdown格式"""
        rows = []
        for row in table.rows:
            cells = []
            for cell in row.cells:
                cells.append(cell.text.strip().replace('\n', ' '))
            rows.append(cells)

        md_rows = []
        for idx, row in enumerate(rows):
            md_rows.append("| " + " | ".join(row) + " |")
            if idx == 0:
                md_rows.append("| " + " | ".join(["---"] * len(row)) + " |")

        return "\n".join(md_rows) + "\n\n"
```

#### PDF解析器 - 使用字体大小检测标题

```python
class PDFParser(DocumentParser):
    def _detect_headings_by_font_size(self, lines_data: list) -> list:
        """根据字体大小检测标题，返回带标题标记的文本"""
        # 统计字体大小分布
        size_counts = {}
        for fs in font_sizes:
            rounded = round(fs)
            size_counts[rounded] = size_counts.get(rounded, 0) + 1

        # 找出最常见的字体大小（正文大小）
        max_count = max(size_counts.values())
        body_sizes = [size for size, count in size_counts.items() if count >= max_count * 0.3]
        body_size = max(body_sizes) if body_sizes else max(size_counts.keys())

        # 找出比正文明显大的字体大小（可能是标题）
        heading_sizes = sorted([size for size in size_counts.keys() if size > body_size], reverse=True)

        result = []
        for line in lines_data:
            text = line["text"]
            font_size = round(line["font_size"])

            # 判断是否是标题
            is_heading = font_size > body_size
            is_centered_heading = line["is_centered"] and len(text) < 80 and len(text) > 2

            if is_heading or is_centered_heading:
                if heading_sizes and font_size >= heading_sizes[0]:
                    level = 1
                elif len(heading_sizes) > 1 and font_size >= heading_sizes[1]:
                    level = 2
                else:
                    level = 2  # 默认二级标题

                result.append(f"{'#' * level} {text}")
            else:
                result.append(text)

        return result
```

---

## 2. Neo4j 核心检索语句

### 2.1 关键Cypher查询

**位置**：`backend/app/services/neo4j_graph.py`

#### 添加实体节点

```cypher
MERGE (e:Entity {id: $entity_id})
SET e.name = $name,
    e.type = $entity_type,
    e.updated_at = timestamp()
```

#### 添加知识条目节点

```cypher
MERGE (k:Knowledge {id: $knowledge_id})
SET k.title = $title,
    k.content = $content,
    k.category = $category,
    k.tags = $tags,
    k.updated_at = timestamp()
```

#### 添加关系边

```cypher
MATCH (source {id: $source_id})
MATCH (target {id: $target_id})
MERGE (source)-[r:RELATES {type: $relation_type}]->(target)
SET r.weight = $weight,
    r.updated_at = timestamp()
```

#### 获取邻居节点（深度=1）

```cypher
MATCH (n {id: $node_id})-[r]->(neighbor)
WHERE r.type = $relation_type  -- 可选过滤
RETURN neighbor.id AS node_id,
       neighbor.name AS name,
       neighbor.type AS type,
       r.type AS relation,
       r.weight AS weight,
       1 AS distance
LIMIT $limit
```

#### 获取多跳邻居

```cypher
MATCH path = (n {id: $node_id})-[*1..{depth}]-(neighbor)
WHERE NOT n = neighbor
WITH path, neighbor, length(path) AS pathLength
ORDER BY pathLength
LIMIT $limit
RETURN neighbor.id AS node_id,
       neighbor.name AS name,
       [r IN relationships(path) | r.type] AS relations,
       pathLength AS distance,
       [n IN nodes(path) | n.id][0..-1] AS path
```

#### 查找两点间路径

```cypher
MATCH path = (source {id: $source_id})-[*1..{max_hops}]-(target {id: $target_id}})
RETURN [n IN nodes(path) | n.id] AS path,
       [r IN relationships(path) | r.type] AS relations
ORDER BY length(path)
LIMIT 10
```

#### 获取相关概念（按度中心性排序）

```cypher
MATCH (e:Entity)
WHERE e.name CONTAINS $concept OR e.type = 'concept'
RETURN e.id AS node_id,
       e.name AS title,
       e.type AS type,
       size((e)-->) AS out_degree,
       size((e)<--) AS in_degree
ORDER BY out_degree + in_degree DESC
LIMIT $limit
```

#### PageRank计算

```cypher
CALL gds.pageRank.write({
    nodeProjection: 'Entity',
    relationshipProjection: {
        RELATES: {type: 'RELATES', properties: 'weight'}
    },
    dampingFactor: $damping,
    maxIterations: $iterations,
    writeProperty: 'pagerank'
})
YIELD nodePropertiesWritten
RETURN nodePropertiesWritten
```

#### 获取图谱统计信息

```cypher
-- 节点和边统计
MATCH (e:Entity)
OPTIONAL MATCH (e)-[r]->()
RETURN count(DISTINCT e) AS node_count,
       count(DISTINCT r) AS edge_count

-- 实体类型分布
MATCH (e:Entity)
RETURN e.type AS type, count(e) AS count

-- 关系类型分布
MATCH ()-[r]->()
RETURN r.type AS type, count(r) AS count
```

### 2.2 支持的关系类型

| 关系类型 | 含义 |
|----------|------|
| `is_a` | 包含关系 |
| `part_of` | 组成关系 |
| `uses` | 使用关系 |
| `caused_by` | 因果关系 |
| `related_to` | 相关关系 |
| `depends_on` | 依赖关系 |
| `contrasts_with` | 对比关系 |
| `located_at` | 位于关系 |
| `implements` | 实现关系 |
| `contains` | 包含关系 |

### 2.3 支持的实体类型

| 实体类型 | 含义 |
|----------|------|
| `concept` | 概念 |
| `technique` | 技术 |
| `tool` | 工具 |
| `vulnerability` | 漏洞 |
| `event` | 事件 |
| `knowledge` | 知识条目 |

---

## 3. 领域专属提示词模板

### 3.1 系统提示词

**位置**：`backend/app/services/enhanced_rag_engine.py` (L88-113)

```python
SYSTEM_PROMPT = """你是网络安全领域的专业教学助手"网安卫士"。

你的职责是：
1. 准确回答网络安全相关问题
2. 使用简洁易懂的语言解释复杂概念
3. 提供实际案例和代码示例
4. 标注答案的知识来源
5. 如不确定，明确告知用户

专业知识领域：
- 网络基础：TCP/IP协议、网络攻防原理
- Web安全：SQL注入、XSS、CSRF、SSRF等漏洞原理与防御
- 系统安全：操作系统加固、权限管理、安全配置
- 密码学：对称加密、非对称加密、哈希算法、数字签名
- 渗透测试：信息收集、漏洞利用、后渗透测试
- 应急响应：事件分析、取证调查、溯源处置
- 数据安全：数据加密、脱敏、隐私保护
- 移动安全：Android/iOS安全、应用加固

回答要求：
- 结构清晰，使用标题、列表等格式
- 复杂概念提供图示说明
- 包含相关的安全警告和最佳实践
- 引用可信的知识来源
- 如果问题超出网络安全领域，请说明并尝试提供相关建议"""
```

### 3.2 安全专家角色提示

```python
SECURITY_EXPERT_PROMPT = """你是一位资深的网络安全专家，具有丰富的教学经验和实战经历。
你擅长将复杂的安全概念用通俗易懂的方式解释清楚。
你会结合实际案例来说明抽象的概念。
你会特别注意提醒用户潜在的安全风险和防护措施。
你的目标是帮助用户真正理解网络安全的原理，而不是简单地给出答案。"""
```

### 3.3 用户查询Prompt构建

```python
if context:
    user_prompt = f"""基于以下网络安全领域的参考资料回答用户问题。
如果上下文中没有相关信息，请基于你的网络安全知识回答，但不要生成果断性的结论。

参考资料：
{context}

用户问题：{query}

请结合参考资料给出准确、专业的回答。
回答要求：
1. 优先使用参考资料中的信息
2. 明确标注答案来源（使用【参考来源】标注）
3. 对于不确定的内容，说明基于何种原理推断
4. 提供相关的安全建议和最佳实践"""
else:
    user_prompt = f"""请回答以下网络安全领域的问题：
问题：{query}
注意：如果问题超出网络安全领域范围，请明确说明。"""
```

### 3.4 追问建议Prompt

```python
prompt = f"""基于以下网络安全问题，生成3个相关的追问建议。
追问应该：
1. 深入探讨原问题的某个方面
2. 涉及相关的实际应用场景
3. 询问原理或最佳实践

问题：{query}

请只输出追问建议，每行一个，格式为"追问：xxx"。"""
```

---

## 4. 大语言模型输出安全控制

### 4.1 防止误导性内容的核心机制

**位置**：`backend/app/services/enhanced_rag_engine.py`

#### 严格的指令系统

```python
# 明确职责边界 - 不能回答非网络安全问题
- 如果问题超出网络安全领域，请说明并尝试提供相关建议

# 要求标注来源 - 防止虚构
回答要求：
- 明确标注答案来源（使用【参考来源】标注）

# 不确定时的处理 - 防止过度自信
- 如不确定，明确告知用户
- 对于不确定的内容，说明基于何种原理推断
```

### 4.2 置信度计算机制

```python
def _calculate_confidence(self, retrieved_docs: List[Dict]) -> float:
    """计算答案置信度"""
    if not retrieved_docs:
        return 0.0

    # 综合考虑相似度分数
    scores = []
    for doc in retrieved_docs:
        sim = doc.get("similarity", 0)        # 向量检索分数
        rerank = doc.get("rerank_score", 0)  # 重排序分数
        cross = doc.get("cross_score", 0)     # 交叉编码器分数

        # 双重验证：原始检索相似度 vs 重排序相似度
        combined = 0.3 * sim + 0.3 * rerank + 0.4 * cross if cross else sim
        scores.append(combined)

    avg_score = sum(scores) / len(scores)
    # 转换为置信度（0-1）
    confidence = min(avg_score * 1.5, 1.0)
    return round(confidence, 3)
```

### 4.3 重排序验证机制

```python
class Reranker:
    def rerank(self, query: str, documents: List[Dict], top_k: int = None) -> List[Dict]:
        """对检索结果进行重排序"""
        # 使用 SecBERT 交叉编码器进行精细相似度计算
        similarity = self.embedding_service.embedding_model.similarity(query, text)

        # 双重验证：原始检索相似度 vs 重排序相似度
        combined_score = 0.4 * original_score + 0.6 * similarity

        scored_docs.append({
            **doc,
            "rerank_score": combined_score,
            "original_score": original_score,
            "cross_score": similarity
        })

        # 按综合分数排序
        scored_docs.sort(key=lambda x: x["rerank_score"], reverse=True)
        return scored_docs[:top_k]
```

### 4.4 多路召回融合

```python
def retrieve(self, query: str, top_k: int = None) -> List[Dict]:
    """混合检索：向量检索 + 知识图谱检索 + RRF融合"""
    all_results = {}

    # 1. 向量检索（语义相似）
    vector_results = self.vector_store.search(query, top_k=top_k * 2)
    for rank, item in enumerate(vector_results):
        weight = Config.VECTOR_WEIGHT * (1 / (60 + rank + 1))
        all_results[item["id"]] = {
            "id": item["id"],
            "score": weight,
            "source": "vector",
            "similarity": item.get("similarity", 0)
        }

    # 2. 知识图谱检索（实体关系）
    graph_results = self.knowledge_graph.get_neighbors(query, depth=Config.GRAPH_MAX_HOPS)
    for rank, item in enumerate(graph_results):
        weight = Config.GRAPH_WEIGHT * (1 / (60 + rank + 1))
        all_results[node_id]["score"] += weight

    # 3. RRF融合排序 (Reciprocal Rank Fusion)
    # 综合多个检索源的结果，防止单一来源的偏差
```

### 4.5 降级策略

```python
# 当RAG服务不可用时，自动降级为简单推荐
# 参见 knowledge.py get_related_knowledge()

except Exception as e:
    print(f"相关知识推荐失败: {e}")

    # 降级：使用简单的同分类推荐
    fallback_items = KnowledgeItem.query.filter(
        KnowledgeItem.category_id == item.category_id,
        KnowledgeItem.id != item_id,
        KnowledgeItem.status == "published"
    ).order_by(KnowledgeItem.view_count.desc()).limit(top_k).all()

    return jsonify({
        "items": [it.to_dict(include_content=False) for it in fallback_items],
        "fallback": True
    }), 200
```

---

## 5. 系统架构总结

### 5.1 文档上传流程

```
┌─────────────────────────────────────────────────────────────────┐
│                      文档上传流程                                │
│  FileUpload → DocumentParserFactory → [PDF/Word/HTML/MD/TXT]  │
│                              ↓                                   │
│                      TextCleaner → KnowledgeItem                │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 RAG安全控制体系

```
┌─────────────────────────────────────────────────────────────────┐
│                      RAG安全控制体系                             │
│  1. PromptTemplate: 明确职责边界、来源标注要求                  │
│  2. 置信度计算: 多维度分数加权，防止低置信度输出                 │
│  3. 重排序验证: 交叉编码器二次确认                              │
│  4. 多路召回: 向量+图谱双源，防止单源偏差                       │
│  5. 降级策略: 服务不可用时自动降级                             │
└─────────────────────────────────────────────────────────────────┘
```

### 5.3 核心技术栈

| 组件 | 技术选型 |
|------|----------|
| 后端框架 | Flask + Flask-JWT-Extended |
| 数据库 | MySQL + SQLAlchemy |
| 向量数据库 | ChromaDB |
| 图数据库 | Neo4j (优先) / NetworkX (备用) |
| LLM | MiniMax / 通义千问 |
| 嵌入模型 | SecBERT (shibing624/text2vec-base-chinese) |
| 文档解析 | pdfplumber, python-docx, BeautifulSoup |

---

*文档生成时间：2026-04-20*
